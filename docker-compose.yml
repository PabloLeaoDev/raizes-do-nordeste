version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: raizes-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: raizes
    ports:
      - "5432:5432"